apiVersion: v1
kind: Service
metadata:
  name: devops-cart-app
spec:
  selector:
    app: devops-cart-app  
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080


